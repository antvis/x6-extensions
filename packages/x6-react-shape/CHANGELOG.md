# 变更日志

## 3.0.1 - 2025-11-27

### 重大变更

- 对齐 `@antv/x6@^3.x`，不兼容旧版 `x6@^2`。
- 要求 `react@>=18`、`react-dom@>=18` 作为 peer 依赖。

### 新特性

- 在 X6 v3 环境下渲染 React 组件的能力与类型定义完善。

### 维护

- 构建与打包配置更新：Rollup 3、Vite 7。
- UMD 产物：`dist/x6-react-shape.min.js`，全局名：`X6ReactShape`。
